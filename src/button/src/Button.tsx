import {h, defineComponent } from "vue";

const Button = defineComponent({
  name: 'Button',
  props: {},
  setup (props) {

  },
  render () {
    return (
      <div>这是一个按钮</div>
    )
  }
})

export default Button